This is a simple form for gathering basic user info.
It the taret is unset so all the name value pare info goes to void!

# commit: basic HTML structure added.

the commit message is self explanetory!